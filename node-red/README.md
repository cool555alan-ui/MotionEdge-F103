# MotionEdge Phase 7 Node-RED

本Flow仅使用Node-RED核心MQTT、Function、HTTP、Template、WebSocket节点，不要求Dashboard插件。
导入前使用`tools/import-node-red-phase07.ps1`备份并合并现有Flow，不覆盖用户内容。页面地址为
`http://127.0.0.1:1880/motionedge`，指标接口为`/motionedge/api/status`和`/motionedge/api/metrics`。
